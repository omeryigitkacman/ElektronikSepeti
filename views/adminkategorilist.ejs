<div class="admin-all-con-extra">
    <%- include('adminnav'); -%>
      <div class="admin-home">
        <% if(sessionFlash) {%>
          <div class="<%=sessionFlash.type %>">
              <%=sessionFlash.message %>
          </div>
          <%}%>
          <div class="d-flex justify-content-between w-100 align-items-center">
            <h3 class="m-4">Kategoriler</h3>
            <a href="/admin/kategoriekle" class="btn btn-info mr-5 text-white"> <i class="fa-solid fa-plus mr-1"></i> Yeni Kategori Ekle</a>
          </div>
          <table class="table">
              <thead>
                  <tr scope="row">
                      <td>Id</td>
                      <td>Kategori Adı</td>
                      <td>Güncelle</td>
                      <td>Sil</td>
                  </tr>
              </thead>
              <tbody>
                <% result.forEach(kategori => { %>
                  <tr class="das">
                    <td><%- kategori.id%></td>
                    <td><%- kategori.KategoriName%></td>
                    <td><a href="/admin/kategoriguncelle/<%- kategori.id%>" class="btn-primary btngüncels" >Güncelle</button></td>
                    <td><a href="/admin/kategorisil/<%- kategori.id%>" class="btn-danger btngüncels">Sil</button></td>
                </tr>
                 <% }) %>
                
          </table>
      </div>
    </div>
  
    <script>
      //Aadmin sayfasındaki menünün listlelerinin olayları BAŞLAR
      var adminMenuLink = document.querySelectorAll(".admin-menu-link");
      adminMenuLink.forEach((e) => {
        var adminMenuIcon = e.querySelector(".admin-menu-effect-icon");
        e.addEventListener("mouseenter", () => {
          adminMenuIcon.style.transform = "translate(0%, 0%) scale(1)";
        });
        e.addEventListener("mouseleave", () => {
          adminMenuIcon.style.transform = "translate(0%, 0%) scale(0)";
        });
      });
      //Aadmin sayfasındaki menünün listlelerinin olayları BİTER
    </script>